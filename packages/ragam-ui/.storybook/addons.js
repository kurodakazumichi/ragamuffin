/**
 * 使用するstorybookのaddonを登録する。
 * これはお約束みたいなものなので新たにaddonを追加した場合はここに列挙していくことになる。
 */
import '@storybook/addon-knobs/register';
import '@storybook/addon-viewport/register';
import '@storybook/addon-actions/register';